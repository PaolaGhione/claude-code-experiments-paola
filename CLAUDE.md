# Project Instructions

## LangGraph

Always use the context7 MCP server (`mcp__context7__resolve-library-id` then `mcp__context7__query-docs`) for every LangGraph question — API usage, concepts, debugging, version details.
